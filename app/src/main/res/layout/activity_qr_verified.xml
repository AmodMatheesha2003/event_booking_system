<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="20dp"
    android:background="@color/lightGreen">

    <TextView
        android:id="@+id/approvalStatusText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Ticket Verified"
        android:textSize="35sp"
        android:textColor="@color/greenasuccess"
        android:textStyle="bold"
        android:layout_marginBottom="50dp"/>

    <ImageView
        android:id="@+id/verifiedIcon"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:src="@drawable/verified"
        android:contentDescription="Verified Icon"
        android:layout_marginBottom="40dp"/>

    <TextView
        android:id="@+id/confirmationText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="This ticket is valid. The holder can enter the event."
        android:textSize="15sp"
        android:textStyle="bold"
        android:textColor="@color/light_black"
        android:layout_marginTop="15dp"/>

    <TextView
        android:id="@+id/eventstatus"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Loading...."
        android:textSize="15sp"
        android:textColor="@color/light_black"
        android:layout_marginTop="15dp"/>

    <Button
        android:id="@+id/qrcontinueButton"
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:text="Continue"
        android:textSize="16sp"
        android:backgroundTint="@color/greenasuccess"
        android:textColor="@android:color/white"
        android:layout_marginTop="50dp"
        android:padding="12dp"/>
</LinearLayout>
